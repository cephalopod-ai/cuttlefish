# Keep npm clean for repo-local installs and ensure optional dependencies stay installed.
include=optional

# The manifest declares Node 24; installs warn rather than enforce the range.
engine-strict=false
