# TODO Ledger

This is the authoritative active Cuttlefish backlog for this checkout. Closed
defects and completed TODOs are retained, with their evidence, in
[TODO_HISTORY.md](TODO_HISTORY.md); they do not remain in this active table.

| ID | Status | Priority | Area | Item | Source | Opened | Last Evidence | Exit Criteria |
|---|---|---|---|---|---|---|---|---|
| EA-CONTRACT-001 | unverified integration | P2 | evidence/upstream compatibility | Validate a narrow adapter when upstream runtime schemas, portable fixtures and implementation handoffs are explicitly available. | Evidence/authority implementation request; [handoff](evidence-execution-authority.md) | 2026-09-16 | Declarative guidance v1.0 was available; independent Cuttlefish version-1 contracts and generic `CUT-EA-*` scenarios ship without a private service dependency. No Gosling/Muninn runtime integration or strict downstream knowledge reader was exercised. Existing role-to-skill questions in `ROLE-SKILL-001` remain unchanged. | Record actual versions/fixture IDs, execute compatible portable fixtures and strict-reader compatibility tests, and disclose unsupported fields/consumer paths; do not infer upstream conformance from local guards. |
| EA-CONTAINMENT-001 | design follow-up | P2 | engines/isolation | Select stronger OS/service isolation if work must protect same-user private files, credentials or native network actions. | [Capability limits](evidence-execution-authority.md#capability-and-delivery-limits) | 2026-09-16 | Codex batch restrictive argument assembly and inert protected dispatch/refusal pass locally. Gateway credentials/state remain accessible in principle to same-user native tools; other adapters refuse required read-only dispatch. No paid/native containment demonstration or provider authentication change was made. | Commission a supported boundary, prove its actual filesystem/network/credential restrictions with owned native fixtures, and validate PTY/subscription compatibility before claiming stronger containment. |
| EA-DELIVERY-001 | design follow-up | P2 | connectors/reconciliation | Extend native connector account revision and durable delivery/reconciliation capabilities if guarantees beyond the automatic web-turn relay are required. | [Delivery limits and recovery](evidence-execution-authority.md#capability-and-delivery-limits) | 2026-09-16 | Automatic relay binds current run/payload/destination and retains unknown acknowledgements; knowledge outbox fingerprints destination/credential revision and quarantines uncertainty. Managed native sends and explicit send APIs lack this relay's durable deduplication; connector account replacement is not represented by the adapter. Recovery acknowledgement never re-arms an uncertain queue row. | Define account/destination revision and stable operation/receiver acknowledgement contracts, validate native/explicit sends and operator reconciliation with local doubles, and keep unknown external effects distinct from exactly-once claims. |
| PLAY-RESUME-20260905-R22 | open | P2 | sessions/background-result | Trace and repair native Claude background task completion/result reconciliation. | September4–5 resumed playtest, MH-03 | 2026-09-05 | Native Agent output contains final marker, but normalized child/one parent callback retain foreground marker. Upstream streams were observed and callback deferred; nested background Bash complicates root-cause attribution. See local resumption session log and closing audit. | A controlled native task outlives foreground completion, then produces exactly one correctly attributed final normalized result/callback after actual task completion; nested-task and adjacent ordinary-turn controls pass. |
| RSP-CUT-001 | open | P1 | repository-posture/branches | Protect `main` with a branch rule or ruleset and required healthy checks. | 2026-08-13 agent-skills audit, finding RSP-CUT-001 | 2026-08-13 | GitHub API: `main` is not protected (`404`) and repository rulesets are empty (`[]`). Requires a GitHub admin settings mutation. See [CONTROL_PLANE_RUNBOOK.md](CONTROL_PLANE_RUNBOOK.md) for the exact ruleset command. | A readback proves `main` protection/rulesets require the intended CI and security checks, restrict destructive/direct updates, and preserve an explicitly documented emergency path. |
| SEC-CUT-016 | open | P2 | security/detection | Land and remotely validate the repaired license-free Gitleaks gate; enable GitHub push protection or explicitly accept its absence. | 2026-08-13 agent-skills audit, finding SEC-CUT-016 | 2026-08-13 | Source installs pinned Gitleaks CLI v8.30.1 from source (no license needed) and runs a full-history scan; **the hosted scan is now green** — run `31923221858` (2026-08-16) succeeded, superseding the pre-repair failure `31611836884`. The earlier failure was the deprecated paid-license `gitleaks-action`; it is replaced by the CLI install. **Only GitHub-native push protection remains off** (`security_and_analysis` is `null`). See [CONTROL_PLANE_RUNBOOK.md](CONTROL_PLANE_RUNBOOK.md). | A maintained scanner executes on push/PR/history (Gitleaks — done, green), the latest hosted scan is green (done), and push protection is enabled or its absence is explicitly accepted. |
| RSP-CUT-002 | open | P2 | repository-posture/releases | Add production-environment protection and branch/tag policy around npm publication. | 2026-08-13 agent-skills audit, finding RSP-CUT-002 | 2026-08-13 | GitHub API reports `npm-production.protection_rules=[]`, `deployment_branch_policy=null`, and `can_admins_bypass=true`; `release-npm.yml` consumes the environment. Requires a GitHub admin settings mutation. See [CONTROL_PLANE_RUNBOOK.md](CONTROL_PLANE_RUNBOOK.md) for the reviewer + branch-policy command. | Readback proves the environment has the selected reviewer/wait/branch-or-tag safeguards, bypass posture is deliberate, and a release rehearsal exercises them. |
| REL-CUT-001 | open | P2 | release/state-reconciliation | Restore npm publication authority, complete v0.23.7, and remove the stale duplicate draft after publication state is verified. | 2026-08-13 agent-skills audit, finding REL-CUT-001 | 2026-08-13 | **Root cause confirmed from failed run `31612431897` logs:** `npm tokens that bypass 2FA are being restricted` followed by `E404 ... you do not have permission`. The `NPM_TOKEN` is not a publish-capable/2FA-compliant automation token — an auth/2FA authority failure, not a missing package (`npm view cuttlefish-cli version` returns `0.23.6`; package exists). CLI manifest is `0.23.7`, tag `v0.23.7` exists, Homebrew is `0.23.6`, duplicate draft release still present. See [CONTROL_PLANE_RUNBOOK.md](CONTROL_PLANE_RUNBOOK.md) for regen-token + rerun + draft-delete steps. | npm, tag, package manifest, release assets, Homebrew formula, and release docs agree on one installable version; duplicate draft disposition is recorded and downstream workflows are green. |
| DEP-CUT-002 | open | P2 | dependencies/dev-tooling | Land the validated `brace-expansion` overrides and confirm Dependabot alert convergence. | 2026-08-13 agent-skills audit, finding DEP-CUT-002 | 2026-08-13 | Overrides are on `main` and pushed: `package.json` pins `brace-expansion@<2.0.0` → `1.1.18` and `>=5.0.0 <5.0.9` → `5.0.9`; frozen install, full/prod audits, lint, tests, and build pass with zero advisories (green on `80157f6`). **Cannot confirm Dependabot alert 15 state** — the alerts API returns `403` (token lacks `security_events` scope); must be checked in the UI or with a scoped token. See [CONTROL_PLANE_RUNBOOK.md](CONTROL_PLANE_RUNBOOK.md). | Full dependency audit has no unaccepted high advisory, production audit stays clean, lint/tests pass, and Dependabot alert 15 is closed or covered by a governed exception. |
| ARB-CUT-001 | open | P2 | orchestration/authority | Decide whether to build the multi-supervisor authority and arbitration surface the 16 `SA-*` scenario cards target. | 2026-09-05 playtest final matrix (SA-02…SA-30 failures) | 2026-09-05 | Current Team supports writable project messages and structured recipients but not durable room collision, multi-supervisor authority, or priority/conflict generation. UI and source were inspected; these are absent features, not defects. See [2026-09-05-playtest-final-matrix.md](audits/2026-09-05-playtest-final-matrix.md). | The operator either commissions the surface (then the cards run against it) or records the target contract as out of scope and the cards are retired from the standing library. |
| A2A-CONF-001 | open | P2 | a2a/list-tasks | Decide whether to break the public page-token format to reach A2A 1.0 List Tasks conformance. | 2026-09-02 A2A federation findings repair, residual | 2026-09-02 | Exposed page tokens encode an offset and list order uses persisted task update time rather than the projected status timestamp the spec requires. Pre-existing; not introduced by the resource-bounding patch. Repair changes a public token format, so it is a compatibility decision. | Either conformant keyset tokens and status-timestamp ordering ship with a stated compatibility break, or the gap is accepted and documented as a deliberate non-conformance. |
| ROLE-SKILL-001 | open | P2 | orchestration/roles-skills | Answer the role↔skill binding questions before any schema work (COA-B2). | 2026-08-31 roles + skills architecture review, §6.3 and §7 | 2026-08-31 | COA-B1 (declarative role `kind`) is **done** — see [2026-09-05-queued-defect-repair.md](audits/2026-09-05-queued-defect-repair.md). B2 still needs B2a (skill names only) vs B2b (inlined skill text, requiring a screening gate and content pinning) chosen, plus the seven §7 questions: what Tagteam is, whether skills route or only instruct, missing-skill policy, conflict precedence, third-party skill trust, org rank vs role reconciliation, and skill versioning. | The operator records the B2a/B2b choice and answers to the open questions in `DECISION_LOG.md`; only then does role↔skill schema work begin. |
| DOC-CUT-001 | open | P3 | repository-posture/docs | Decide whether to add a public code of conduct. | 2026-09-03 v0.23.9 release readiness, GHR-010 (advisory); corrected 2026-09-16 | 2026-09-03 | `SECURITY.md` and `.github/CONTRIBUTING.md` exist; the README links the contributor guide. Root `CODE_OF_CONDUCT.md` is absent. The earlier claim that both community files were absent was inaccurate. Advisory only — no rule requires a code of conduct; public governance language is a maintainer decision. | A code of conduct exists and is linked, or its absence is recorded as deliberate. |
| TALK-CUT-001 | open | P3 | gateway/compatibility | Decide whether backend Talk is a supported external compatibility API or should be decommissioned. | 2026-08-10 seams-and-edges audit, Giles ledger 0102 | 2026-08-10 | The retired browser Talk workflow keeps a large mutable backend surface with no production dashboard caller. | The disposition is recorded; the surface is then either documented and covered by tests, or removed. |
| REV-BASE-001 | open | P2 | orchestration/review-determinism | Decide how the mid_pair reviewer gets a deterministic baseline for its diff. | 2026-07-10 cloud audit A-F5; reconfirmed 2026-09-05 walkthrough | 2026-07-10 | `review-context.ts` runs a live `git diff HEAD` against the shared `parentSession.cwd`, and role children inherit that cwd, so a concurrent session's edits are attributed to the implementer. Since the original finding it gained a 10s timeout, workspace-root validation, a marked truncation and an explicit `summary_only` degrade with a reason. The remaining fix is architectural — snapshot a baseline ref at implementer start, or route role children through the existing `isolated_worktree` policy — and changes where employee work executes. | A reviewer's diff reflects only the implementer's own changes under a concurrent session, proven by a two-session replay. |
| REV-FAMILY-001 | open | P3 | orchestration/consensus | Decide whether the mid_pair reviewer should default to a different model family than the implementer. | 2026-07-10 cloud audit A-F7 (routing half) | 2026-07-10 | The visibility half is closed — `executionReviewIndependence` now records when a review was not independent. Changing the default routing would break installations with a single signed-in engine, and "which family reviews which" is a policy choice. | Either a documented cross-family default with a single-engine fallback, or an explicit decision to keep operator-chosen routing with the independence marker as the control. |
| TALK-ATTACH-001 | open | P3 | talk/authorization | Confirm or reverse the deliberate parent-ownership skip on talk `attach`/`engage`. | 2026-07-10 cloud audit A-F6 | 2026-07-10 | `talk/delegate.ts` marks the skip intentional ("attachments are soft links") and engage-mode relays are provenance-stamped, so any talk session can adopt any non-talk session by id. This is a recorded deliberate posture, not an oversight; reversing it is an operator decision. | The posture is recorded in `DECISION_LOG.md` as accepted, or an ownership/share-grant check is added with tests. |
| SEC-REV-002 | open | P3 | security/review-context | Decide whether the standing security-reviewer session should retain cross-session context or reset per review. | 2026-09-05 walkthrough | 2026-09-05 | The reviewer runs on one singleton session that is never reset, so each review's prompt — which embeds the reviewed session's transcript tail — accumulates in a session shared across employees and departments. The singleton-employee-session pattern is deliberate; the misattribution risk it created is already closed. | Either the accumulation is recorded as intended standing-reviewer context, or reviews reset/scope the session with tests covering both. |
| SEC-MCP-ARGV-001 | design follow-up | P2 | MCP/Codex credential transport | Select and implement a way to preserve per-server MCP environment values without embedding secret values in Codex launch arguments. | 2026-09-16 source review, `mcp/resolver.ts:229–245`; `engines/codex.ts:451,462`; `engines/codex-interactive.ts:387` | 2026-09-16 | Stdio MCP `env` values become `-c mcp_servers.<name>.env.<key>="value"` arguments. Some configured values can be credentials. This is a source-level host-process privacy risk; no live credential-bearing process, exploit or external MCP server was exercised. The generated JSON file read guard does not protect argv. A replacement must preserve distinct values for different servers, provider login and child lifecycle. | Secret values are absent from launch argv; native inert-server tests prove independent per-server environments, startup errors and termination behavior; provider compatibility is validated before claiming live coverage. |
| OPS-PID-IDENTITY-001 | design follow-up | P2 | lifecycle/process ownership | Evaluate persisted process birth identity for stale positive PID reuse. | 2026-09-16 source re-audit, `gateway/lifecycle.ts:getStatus/signalGateway/acquireDaemonLock` | 2026-09-16 | Strict PID admission now rejects malformed/nonpositive values. A valid positive PID file still identifies its owner by PID and liveness; OS PID reuse after abrupt termination is not distinguished by a process birth identity. No unrelated live process was inspected or signalled to test this concern. Existing private PID-file format and shutdown behavior remain compatible. | A compatible ownership record distinguishes process generation before signalling, preserves foreground/background/restart and legacy recovery, and passes controlled owned-process tests on supported platforms. |

Most existing entries wait on an operator decision or an operator-held credential.
The native background-result investigation and MCP argument transport follow-up
also require engineering and controlled compatibility evidence. Process birth identity is a separate lifecycle design follow-up.

September 16 unauthenticated public readback still shows npm `0.23.6`, the latest
GitHub release `v0.23.7` without assets, and the checked-in Homebrew tarball
`0.23.6`, while this checkout's CLI manifest is `0.23.9`. This updates the local
version fact in `REL-CUT-001` without asserting that its old failure logs or
private admin/security observations were freshly verified. Publication and
admin-setting mutations remain outside the operational repair authorization.

The control-plane items (`RSP-CUT-001`, `SEC-CUT-016`, `RSP-CUT-002`,
`REL-CUT-001`, `DEP-CUT-002`) remain the blocking release backlog.
`PERF-CUT-001` through `PERF-CUT-003` were completed on 2026-08-17 and are
retained with their evidence in [TODO_HISTORY.md](TODO_HISTORY.md).

The 2026-09-05 orchestration/data-flow walkthrough closed `A-F7-VISIBILITY`,
`A-F9`, `CB-PARK-001` and `SEC-REV-001`; see
[2026-09-05-orchestration-dataflow-walkthrough.md](audits/2026-09-05-orchestration-dataflow-walkthrough.md).

The 2026-09-05 queued-defect repair pass closed `B-INV-001`/`MOD-B-CS-01`,
roles/skills finding `F2` (COA-B1), the Browserslist advisories, the two
recorded `config-schema` duplications, and an intermittent `resolve-bin` probe
failure; see [2026-09-05-queued-defect-repair.md](audits/2026-09-05-queued-defect-repair.md)
and [TODO_HISTORY.md](TODO_HISTORY.md).

The five control-plane entries require selected GitHub/npm changes or privileged
readback; this run did not authorize publication or admin-setting changes.
The earlier sandbox limitation is historical, not the current permission model.
`SEC-CUT-015`, `SEC-CUT-017`, `ARC-CUT-008`, `OPS-CUT-001`,
and `TST-CUT-004` met their exit criteria and moved to
[TODO_HISTORY.md](TODO_HISTORY.md). `TST-CUT-004` is now satisfied: the
scroll-resize repair is on `main` and the GitHub-hosted CI is green on
`80157f6` (run `31923221899`): `build`, `typecheck`, `unit-tests`, `windows`,
`e2e`, `Run Gitleaks Scan`, and `giles` all report `success`. The exact,
copy-pasteable commands to close the remaining control-plane items are in
[CONTROL_PLANE_RUNBOOK.md](CONTROL_PLANE_RUNBOOK.md).
