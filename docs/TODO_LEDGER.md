# TODO Ledger

This is the authoritative active Cuttlefish backlog for this checkout. Closed
defects and completed TODOs are retained, with their evidence, in
[TODO_HISTORY.md](TODO_HISTORY.md); they do not remain in this active table.

| ID | Status | Priority | Area | Item | Source | Opened | Last Evidence | Exit Criteria |
|---|---|---|---|---|---|---|---|---|
| PLT-007 | open | P2 | cli-ux | `cuttlefish start` on a running gateway silently restarts it instead of no-op; a stray `start` bounces active sessions | LC-04 variation, `docs/test_scenarios/runs/2026-07-25-fable5-smoke.md` | 2026-07-25 | Confirmed 2026-07-25: "Gateway already running — restarting in background", new PID observed | `start` against a healthy gateway leaves the PID unchanged, or LC-04 is amended to document intentional restart semantics with a confirmation/flag |
| PLT-008 | open | P3 | cli-ux | `cuttlefish skills find ""` spawns the npm registry client on an empty query (network/subprocess side effect before validation) | CL-02 branch 4, `docs/test_scenarios/runs/2026-07-25-fable5-smoke.md` | 2026-07-25 | Confirmed 2026-07-25: npm began installing `skills` package on empty-string query | Empty/whitespace query returns a one-line usage error with non-zero exit before any subprocess spawn |
| PLT-009 | open | P2 | cli-ux | `--json` error paths emit prose/empty stdout (`unpair <bogus> --json` → exit 1, empty stdout, prose on stderr), breaking the machine-readable contract | CL-03 step 2, `docs/test_scenarios/runs/2026-07-25-fable5-smoke.md` | 2026-07-25 | Confirmed 2026-07-25: stdout empty, "Paired browser not found" on stderr | Error case emits a JSON object on stdout (status/message) with non-zero exit, verified for every `--json` command |
| PLT-010 | open | P1 | install/docs | The documented source-install command `pnpm setup` invokes pnpm's builtin setup, edits the user's shell profile, and never runs Cuttlefish initialization | LC-01, `docs/audits/082026/2026-08-12-cuttlefish-scenario-playtest.md`; `README.md:58,342`; `package.json:19` | 2026-08-12 | Confirmed 2026-08-12 on pnpm 10.6.4: a `PNPM_HOME` block was appended to `.zshrc`; `pnpm run setup` then initialized 39 Cuttlefish items correctly | A clean-shell replay of the documented command initializes the selected disposable home, makes no shell-profile edits, and remains idempotent |
| PLT-011 | open | P1 | engine/codex | Codex is detected and offered in the model picker, but every run fails before turn one because Codex 0.144.1 rejects `--ask-for-approval` | MS-01, `docs/audits/082026/2026-08-12-cuttlefish-scenario-playtest.md`; `packages/cuttlefish/src/engines/codex.ts:42-45` | 2026-08-12 | Session `714d9d5c-1012-486d-b2f1-da684651b9e1` failed with code 2, `totalTurns=0`, and `unexpected argument '--ask-for-approval'` | Ordinary and interactive Codex smoke turns succeed on the supported CLI while retaining explicit sandbox policy; adapter tests assert only accepted flags |
| PLT-012 | open | P1 | cli/lifecycle | Unix port ownership treats Chrome client sockets as listeners after gateway stop, so stopped status is erroneous and normal start is refused | LC-04, CL-04, OD-01, `docs/audits/082026/2026-08-12-cuttlefish-scenario-playtest.md`; `packages/cuttlefish/src/gateway/lifecycle.ts:466-485` | 2026-08-12 | Reproduced on ports 64734, 64735, and 64737: no listener remained, but Chrome PID 838 `CLOSE_WAIT`/`CLOSED` sockets caused "port occupied" and start refusal | Port ownership uses listening sockets only; after stop, status is stopped and start succeeds with foreign client-only sockets, while a real foreign listener is still refused |
| PLT-013 | open | P2 | web/chat | Rapidly double-clicking Send submits one message then silently interrupts it, leaving no assistant response and no error | CH-02, `docs/audits/082026/2026-08-12-cuttlefish-scenario-playtest.md`; `packages/web/src/components/chat/chat-input-composer.tsx:48,143-148` | 2026-08-12 | Claude session ended idle with `lastError=null`; durable history ended at `PLAYTEST-20260812-CH02-DOUBLE` with no assistant message | Repeated double-click tests create one user message and one response, never invoke stop, and create no duplicate backend turn |
| PLT-014 | open | P3 | cli-ux | Bare `cuttlefish` prints usage to stderr and exits 1 even though it is the basic help/discovery path | CL-01, `docs/audits/082026/2026-08-12-cuttlefish-scenario-playtest.md`; `packages/cuttlefish/bin/cuttlefish.ts:24-28,628` | 2026-08-12 | Confirmed 2026-08-12: exit 1, stdout empty, 52 stderr lines; explicit help paths exit 0 | Bare invocation prints discoverability help and exits 0; invalid commands and flags remain non-zero |
