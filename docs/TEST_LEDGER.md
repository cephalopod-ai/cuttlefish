# Test Ledger

| Test Area | Command / Evidence | Last Known Result | Source | Coverage Meaning | Gaps |
|---|---|---|---|---|---|
| Queued defect repair (engine-name drift, role kinds, advisories, duplication, probe flake) | New `shared/__tests__/engine-name-drift.test.ts` and `orchestration/__tests__/role-kinds.test.ts`; extended `run-mode.test.ts` and `resolve-bin.test.ts`; focused orchestration and config-schema suites; `pnpm typecheck`; `pnpm lint`; `pnpm test`; `pnpm build`; `pnpm audit` and `pnpm audit --prod` | passed locally on 2026-09-05: 3,717 tests across 460 files (cuttlefish-cli 340 files / 2,897 passed / 3 intentional skips; web 119 files / 814; contracts 1 file / 6); typecheck, lint and build exited 0; both audits report no known vulnerabilities; full suite run twice to confirm the `resolve-bin` flake is gone | local `queued-defect-repair-20260905`; Giles ledger 0118; `docs/audits/2026-09-05-queued-defect-repair.md` | Pins that the engine membership list has exactly one source (every canonical name is accepted as an `engines.*` key, actually validated, accepted as `engines.default` and under `models`, with the error text matching the gate); that a declared role `kind` overrides the id/capability heuristics in both directions and reaches the real review-only execution controls, while undeclared roles keep their historical classification; and that the `isInstalled` probe's production 2s bound is unchanged while its test seam removes a load-dependent failure. New suites were mutation-checked: disabling declared-kind resolution fails 8 cases, and a 1ms probe timeout reproduces the exact `resolve-bin` assertion. | Local evidence only — no hosted CI, Windows, E2E browser, live engine or connector run, and no Giles compliance certification. The role-kind change is covered by unit and one end-to-end run-mode case, not by a live multi-worker orchestration playtest. Legacy-heuristic behavior is pinned by regression cases, not proven equivalent over every possible role id. |
| Disposable gateway playtest and repairs | Real Chromium browser, scoped built CLI and HTTP API; `pnpm test`; `pnpm typecheck`; `pnpm lint`; `pnpm build`; `CUTTLEFISH_E2E_PORT=62106 pnpm test:e2e`; `git diff --check` | passed locally on 2026-09-04: 454 unit-test files, 3,601 passed / 3 skipped; 9 fixture E2E tests; typecheck, lint, build and diff check passed. Earlier full runs exposed host-binary probe timeouts in six mock/model fixtures; those fixtures now stub availability, preserving assertions and timeouts. | `PLAYTEST-20260904`; Giles ledger 0116; local `2026-09-04-playtest-repairs` audit | Covers four reproduced repairs, no-engine onboarding, CLI error contracts, ticket edit/delete/restore, org/config rejection and persistence, checkpoint decisions/replay/race, pairing/revocation, artifact integrity and gateway lifecycle. Live evidence is separate from fixture E2E results. | The 225-card library is only partially exercised. Signed-in engine, connector, active-work, high-load and authority-arbitration branches are not certified; see the per-card local manifest. Windows and hosted CI were not run. |
| cuttlefish-cli v0.23.9 release gate | Pre/post release-preparation `pnpm typecheck`; `pnpm test`; `pnpm lint`; `pnpm build`; `pnpm --filter cuttlefish-cli verify:package`; `git diff --check`; relative Markdown-link validation; full-history Gitleaks; GitHub CI/Governance/Secret Scanning readback | passed locally on 2026-09-03: backend 334 files / 2,774 passed / 3 intentional skips; web 118 files / 801 passed; contracts 6/6; typecheck, lint, build, diff, links, and package verification passed; package gate verified 2,071 files; current remote checks were green and Gitleaks found no repository leak | local `release-v0.23.9-20260903`; Giles ledger 0115; `docs/changes/releases/v0.23.9.md` | Covers the exact version/tag contract, final release documentation, full workspace behavior, package contents, tracked release links, repository-secret history, and current default-branch automation before the release-preparation commit. | Fresh-clone drill and post-push SHA checks remain pending until the preparation commit exists. `npm-production` publish authority must be refreshed or independently proven before publication. Production dependency audit reports one high `deepmerge-ts` advisory; GitHub also reports one high Browserslist advisory with no currently resolvable Dependabot update. Existing Vite loader/react-swc and React `act(...)` diagnostics remain non-fatal. |
| CLI engine compatibility and model refresh | Provider-native version/update/model probes; focused Claude/Codex/Antigravity/Grok/Ollama/Vibe registry and adapter suites; built-registry inspection; real local Ollama completion; `pnpm typecheck`; `pnpm lint`; `pnpm test`; `pnpm build`; `git diff --check` | passed locally on 2026-09-03: installed Claude Code 2.1.259, Codex 0.153.1, Antigravity 1.1.25, Grok 1.0.13, Ollama 0.33.3, and Vibe ACP 2.24.5 answered their probes; focused compatibility suites passed; root 334 backend files / 2,774 passed / 3 intentional skips; typecheck, lint, and build passed; the rebuilt Ollama adapter returned the exact smoke sentinel with `gemma4:26b` | local `cli-engine-compatibility-refresh-20260903`; Giles ledger 0114; live local CLI and gateway evidence | Covers all 11 registered Cuttlefish engine registrations, setup probing for every registration, bounded live model discovery for Codex, Antigravity, Grok, and Ollama, authoritative removal of configured Codex/Grok IDs omitted by live discovery, exact Ollama tags with embedding-only models excluded, Fable 5.1 and Gemini 3.8 defaults, current Grok defaults, refreshed local config, and upgraded installed provider applications without retaining an old Ollama application copy. | Pi, Kiro, Hermes, Kilo, and Aider were not installed and were not added; no paid live Claude/Codex/Antigravity/Grok/Vibe generation turn was run. Codex 0.153.1 still advertises GPT-5.4, so it remains present until live discovery omits it. Codex doctor reports two pre-existing thread/rollout index mismatches plus expected non-TTY shell limits; the gateway log retains a pre-existing periodic socket-timeout exception outside this repair. |
| A2A 1.0 HTTP+JSON federation | Focused A2A adapter/config/store/lifecycle/outbound route, cross-request, SSRF, scoped-auth, dispatch, redaction, restart cleanup, message-idempotency, and config-drift suites; `pnpm typecheck`; `pnpm lint`; `pnpm test`; `pnpm build`; earlier `pnpm audit --prod --audit-level=low`; official A2A TCK selected HTTP+JSON 1.0 suites; official Python A2A SDK peer round trip; source and independent critic review | passed locally on 2026-09-02: residual-closure focused gate 88/88; root 448 test files / 3,571 passed / 3 intentional skips; typecheck, lint, and build passed. A more concurrent backend-only run reproduced two unrelated five-second timeout flakes; both passed 8/8 in isolation and the subsequent canonical root suite passed. The final independent critic ran 56 focused tests plus package typecheck and reported no implementation blocker. Earlier feature evidence: both `qs` advisories on the new Express path were removed by the workspace override; 42 selected TCK checks passed / 30 transport-inapplicable checks skipped; independent peer discovery/send/completed-task round trip passed | local `a2a-1-0-inbound-adapter-20260902` implementation and post-audit residual repair; Giles ledger 0112 | Covers public Agent Card, per-client bearer/x-api-key auth, AIP-193 HTTP errors, allowlists, durable at-most-once inbound identity, atomic context roots, persisted service routing, native approval round trip, descendant approval and authority boundaries, inbound/outbound cancellation races, terminal-over-cancel settlement, stream/status mapping, outbound HTTPS/SSRF/origin/skill confinement, canonical run ownership, artifact lineage, operator routes, SQL-paged ordinary task listing, bounded dynamic projection batches, abrupt/graceful watcher recovery, opt-in same-message-ID taskless reconciliation with a pinned peer identity and retry ceiling, fail-closed configuration drift, and idempotent local progress/result persistence. | Exact dynamic status/timestamp totals remain O(N) in matching owner/context history even though peak rows and projection fan-out are bounded. A separate pre-existing List Tasks conformance gap remains: exposed page tokens are encoded offsets and ordering follows persisted task update time rather than projected status timestamp; no full List Tasks 1.0 conformance is claimed. Opted-in taskless replay depends on the peer honoring its separately asserted guarantee; older sessions without the new checkpoint fail visibly. JSON-RPC/gRPC/push remain out of scope. The earlier production audit still exited 1 for the pre-existing `mailparser > html-to-text > deepmerge-ts@7.1.6` advisory outside this adapter change. |
| 2026-08-13 agent-skills defect repair campaign | Focused workflow-policy, SSRF/upload, MCP cleanup, route/service, and scroll DOM suites; real Gitleaks history and filtered proposed-tree scans; benign live pinned-fetch probe; frozen install; full/prod dependency audits; `pnpm typecheck`; `pnpm lint`; `pnpm test`; `pnpm build`; `pnpm --filter cuttlefish-cli verify:package`; full E2E plus `scroll.spec.ts --repeat-each=15`; `git diff --check` | passed locally on 2026-08-13: workflow validator plus 14/14 adversarial fixtures; Gitleaks 371 commits and 10.18 MB proposed tree/no leaks; both audits zero advisories; 31/31 focused security/lifecycle tests and post-transport SSRF/upload recheck 21/21; benign HTTPS fetch HTTP 200; 96 route/service tests with 1 intentional skip; 21/21 focused scroll DOM tests; scroll 45/45 and full E2E 9/9; root 438 files / 3,466 passed / 3 skips, typecheck 4/4, lint 3/3, and build; package contents 1,947 files | local `agent-skills-defect-repair-campaign-20260813`; companion audit/session records and Giles ledger 0109 | Closes SEC-CUT-015, SEC-CUT-017, ARC-CUT-008, and OPS-CUT-001 locally; repairs source for dependency, hosted scanner, release honesty, and scroll reliability while preserving their remote exit gates. | No push or hosted post-repair run; `main`, push protection, and `npm-production` remain unprotected; Dependabot alert 15 remains open; v0.23.7 publication recovery and duplicate-draft disposition require release authority. |
| 2026-08-12 comprehensive audit and repair campaign | 39 structurally relevant private-catalog audit lenses; focused security/runtime/CLI/UI regressions; adversarial security and product re-review; `pnpm typecheck`; `pnpm lint`; `pnpm audit --prod --audit-level=low`; `pnpm test`; `pnpm build`; `pnpm test:e2e --reporter=line`; built CLI scenarios; installed Codex 0.144.1 parser probes; `git diff --check` | passed on 2026-08-12: typecheck 4/4, lint 3/3, no known production dependency vulnerabilities, 437 test files / 3,458 passed / 3 intentional skips, build, 9/9 Playwright scenarios, built CLI contracts, and both Codex parser paths | local `codex/comprehensive-audit-repair-20260812` campaign; commits `85e27d0`, `6ddd9be`, `4e1d53d`, and final documentation commit | Closes every confirmed finding from the pass and `PLT-007`–`PLT-014`, covering gateway trust boundaries, lifecycle/scheduling/data invariants, CLI contracts, deletion atomicity, accessibility/UI behavior, dependency/release drift, and Giles adapter deduplication. | Deep-security scanner could not start under system Python 3.9 (`tomllib`/`tomli` unavailable); Giles executable unavailable; no paid live engine or destructive operator-state test; 43/225 playtest cards were exercised; same-user filesystem, border-MTA provenance, and never-settling cron quarantine remain documented residuals. |
| 2026-07-21 playtest defect repair campaign | Focused checkpoint, hanging-screening, Ollama-output, limits-JSON, and model-registry regressions; per-stage full backend suites; `pnpm typecheck`; `pnpm lint`; `pnpm test`; `pnpm build`; `git diff --check`; disposable live gateway and CLI probes | passed on 2026-07-21: typecheck 4/4, lint 3/3, build, and 417 test files / 3,271 passed tests / 1 intentional skip; live probes confirmed coherent checkpoint state, bounded resource attachment with reviewer cleanup, exact clean Ollama output, and strict JSON stdout | local `repair/playtest-defects-20260721` campaign; commits `28c004b`, `cf6da43`, `2e4add1`, `b093fb3` | Closes PT-SC-07 through PT-SC-10 with source-level, regression, adversarial, and live-boundary evidence. | Browser- and specialized-fixture-blocked scenario cards from the source audit remain unexecuted; no remote changes were made. |
| Project/session collaboration prototype | Focused collaboration backend and web suites; `pnpm typecheck`; `pnpm lint`; `pnpm test`; `pnpm test:e2e --reporter=line`; `pnpm build`; `git diff --check`; rebuilt live-daemon Team/Management, `/talk`, and approval-auth probes | passed on 2026-07-21: 47 focused backend tests, 27 focused web tests, 6 contract tests, 765 web tests, 2,490 backend tests with 1 skipped, and 9 Playwright scenarios; live project `#66` surfaced its consolidated feed and Management/default-authority UI with no failed requests or console errors | local SB-CUT-001 completion | Covers recursive project trees, integrity warnings, stable feed projection/cursors, structured Team and Management routing, exact `@all` confirmation, one-turn authority constraints, URL/inspector/tab migration, atomic inactive-tree deletion, scoped-principal isolation, Talk frontend retirement, production bundle deployment, and the local approval-auth retry path. | No live engine message or real project deletion was performed. The in-app browser service and Python Playwright were unavailable, so live read-only UI verification used the repository's installed Node Playwright driver; Talk backend compatibility remains intentionally supported and P2 remains deferred. |
| FleetView Workers DataView | Focused Workers URL/presence/route + axe suites; `pnpm test:e2e --reporter=line`; `pnpm typecheck`; `pnpm lint`; `pnpm test`; `pnpm build`; `git diff --check` | passed on 2026-07-20: 12 focused Worker tests, 7 Playwright scenarios, and all root checks. | local FLEETVIEW-2607-001 implementation | Covers URL restoration/cleanup while retaining unrelated params, derived available/held/working/capacity state, accessible keyboard inspector selection, coordinator/hold context, axe structural semantics, and the built desktop and mobile views. | Browser fixture uses deterministic orchestration data rather than a live daemon; the inspector is observational and deliberately does not change delegation semantics. |
| Active defect repair campaign | Focused regression suites for WeeklySchedule/cron utilities, web-session accounting/stall policy, checkpoints/approvals, orchestration CLI, and Skills add; `pnpm typecheck`; `pnpm lint`; `pnpm test`; `pnpm build`; `git diff --check` | passed on 2026-07-20: all focused suites passed; root typecheck, lint, test, build, and diff check passed. Root web suite: 120 files, 970 tests. | local active-defect repair campaign | Covers deterministic configured-timezone rendering tests, one-time successful web-session accounting, terminal checkpoint decision conflicts, shipped orchestration CLI registrations, and idempotent Skills add results. | Does not run live external engines or make remote changes; FLEETVIEW-2607-001 was subsequently completed; `SB-CUT-001` remains a product decision. |
| Chat sidebar attention legibility | `pnpm --filter @cuttlefish/web test -- src/components/chat/__tests__/sidebar-row-components.test.tsx src/components/chat/__tests__/chat-sidebar-helpers.test.ts src/components/chat/__tests__/sidebar-view-model.test.ts src/lib/rooms/__tests__/grouping.test.ts`; `pnpm typecheck`; `pnpm lint`; `pnpm build`; `git diff --check` | passed on 2026-07-20: focused suite 79/79; root typecheck, lint, and build passed; `git diff --check` passed. Its contemporaneous full suite had one `TST-CUT-003` timezone failure, which the active-defect repair campaign subsequently resolved. | local sidebar attention-legibility repair (`.giles/feature-ledger/giles-ledger-0077-sidebar-attention-legibility-20260720.md`) | Covers color-coded job-state labels on nested (expanded-agent) session rows matching top-level flat rows, an agent row's status dot surfacing the single most urgent child session rather than only the latest/first-unread one, and Department Rooms tracking `needsAttentionCount` separately from `runningCount`. | Rendered browser verification was not performed (no browser-control connection available in this environment); the fix was deployed to the local gateway via rebuild + `cuttlefish restart` but not visually confirmed in-browser. |
| Exploratory playtest scenario library | `docs/test_scenarios/README.md` and cards `01`–`20` | library audited 2026-07-20: 225 uniquely identified cards; 30 SA cards cover authority collisions, supervisor precedence, COO/Parliamentarian arbitration, prioritized messages, deconfliction, fallback, board-independent agent-to-human indicators, grouped chat navigation, and the shared-room mention/topic target; universal evidence/timeout/cleanup gates retained | local scenario-library rigor expansion plus recent-fix and operator-reported gap analysis | Standing operator-facing playtest plan for lived gateway/UI behavior beyond unit/e2e suites; distinguishes implemented authorization/grouping regressions from the not-yet-implemented arbitration, semantic-attention, and writable-room target contracts. | Scenarios are manuals for human/agent playtest passes, not automated CI. Arbitration, semantic FYI/reply indicators, and writable room mentions/topics may currently fail by design gap; live engines, multi-participant fixtures, connector sandboxes, and controllable timing/failure fixtures remain environment-dependent; execution evidence belongs in `docs/audits/` pass reports. |
| Autonomous authorization mode safety guards | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/autonomous-mode.test.ts src/gateway/__tests__/dual-model-verdict.test.ts src/shared/__tests__/autonomous-config.test.ts src/engines/__tests__/codex.test.ts src/engines/__tests__/claude-interactive.test.ts` | passed on 2026-07-20: 68 tests; package tsc + eslint passed; full backend suite 2413 passed / 1 skipped | local autonomous-mode plan-review refactor (feature ledger 0060) | Covers fail-closed project resolution (kill switch, singleton, invalid cwd), exact-cwd matching, the verdict-session recursion stamp, config-load invariants (cwd required, at-most-one profile), judge-only engine flags for both Codex (`--sandbox read-only`) and Claude (`--disallowedTools` denials), verdict JSON parsing, and per-rung error containment in `requestDualModelVerdict`. Protected: security/anti-fake guard — do not delete or demote without an equivalent replacement. | Does not spawn live verdict engines or exercise the checkpoint/org-change auto-resolution end-to-end; the plan's runtime verification steps remain outstanding. |
| COO Fable default and Opus fallback | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/org-hierarchy.test.ts src/cli/__tests__/config-seed.test.ts src/shared/__tests__/model-fallback.test.ts`; `pnpm typecheck`; `pnpm lint`; `pnpm build`; live config/profile readback | passed on 2026-07-20: 51 focused tests; root typecheck, lint, build, restart, and health check passed | local COO execution-profile update | Covers virtual COO profile, fresh-install Fable 5/Medium configuration, and Opus 4.8/Max automatic fallback selection. | Does not invoke a live Claude turn. |
| HR singleton model and effort reuse | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/session-write-routes.test.ts` | passed on 2026-07-20: 18 tests | local chat-profile repair | Covers reusing the HR singleton with an explicit same-engine model and effort update before dispatch; incompatible engine and working-directory overrides remain blocked. | Does not run a live engine turn. |
| Team-change approval origin and authority | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/auth-gate.test.ts src/gateway/__tests__/scoped-token-forbidden.test.ts src/gateway/__tests__/org-changes.test.ts src/gateway/__tests__/hr-steward.test.ts`; `pnpm --filter cuttlefish-cli typecheck` | passed on 2026-07-20: 45 tests; package typecheck passed | local team-approval regression repair | Ensures anonymous callers cannot create unowned org changes, scoped chats can propose but cannot decide, browser/admin operators can decide, and the created approval targets the originating chat. | Does not exercise a live engine-generated proposal; the route and approval binding are unit-covered. |
| Live-playtest repair regressions | `pnpm --filter cuttlefish-cli exec vitest run src/cli/__tests__/instances-safety.test.ts src/cli/__tests__/instances-security.test.ts src/cli/__tests__/skills-manifest.test.ts src/cli/__tests__/start.test.ts src/gateway/__tests__/lifecycle-restart-lock.test.ts src/gateway/__tests__/run-web-session-stall-policy.test.ts src/cron/__tests__/runner.test.ts`; `pnpm --filter @cuttlefish/web exec vitest run src/main.test.tsx` | passed on 2026-07-20: 32 backend and 1 web regression tests | local live-playtest repair campaign | Covers custom-home registry alignment, seeded skills manifest shape, engine-exit error classification, restart coalescing, clean config errors, and unknown-route recovery. | Does not run a live engine or two real daemon processes; those flows remain environment-dependent. |
| Root typecheck | `pnpm typecheck` | passed on 2026-07-21 | local SB-CUT-001 completion | TypeScript checks for `cuttlefish-cli`, `@cuttlefish/web`, and `@cuttlefish/contracts`. | Does not execute runtime flows. |
| Root lint | `pnpm lint` | passed on 2026-07-21 | local SB-CUT-001 completion | ESLint checks package source trees with `--max-warnings=0`. | Does not lint docs. |
| Root unit tests | `pnpm test` | passed on 2026-07-21; contracts: 6 tests; web: 109 files / 765 tests; backend: 305 files / 2,490 passed and 1 skipped. | local SB-CUT-001 completion | Full workspace regression coverage after collaboration persistence, routing, UI, migration, and Talk frontend retirement. | Does not invoke live engines or destructively test a real project deletion. |
| Setup/config CLI tests | `pnpm --filter cuttlefish-cli exec vitest run src/cli/__tests__/setup.test.ts src/cli/__tests__/config-seed.test.ts` | passed on 2026-06-25 | local stewardship run | Covers setup/config seed behavior after aligning the setup Node warning. | Focused subset only. |
| Giles repo standard | `giles repo-check /home/ericl/Work/vscode/public_share/cuttlefish --format pretty` | failed on 2026-06-30 with `finding_count: 6` | local prefork common substrate Stage 0 run | Confirms Giles remains installed and reports current governed repo posture. | Fails on pre-existing `logs` root and `governance/agent_registry.yaml` required-field findings unrelated to the Stage 0 drift-guard patch. |
| CI typecheck + lint | `.github/workflows/ci.yml` typecheck job | configured | `.github/workflows/ci.yml` | Installs with pnpm and runs both `pnpm typecheck` and `pnpm lint`. | CI run result not checked in this session. |
| CI unit tests | `.github/workflows/ci.yml` unit-tests job | configured | `.github/workflows/ci.yml` | Runs `pnpm build` then `pnpm test`. | CI run result not checked in this session. |
| CI build | `.github/workflows/ci.yml` build job | configured; local `pnpm build` passed on 2026-07-20 | `.github/workflows/ci.yml`, local live-playtest repair campaign | Runs the production build after typecheck. | Remote CI run result was not checked in this session. |
| E2E tests | `pnpm test:e2e --reporter=line` | passed on 2026-07-21: 9 tests | `playwright.config.ts`, `scripts/start-e2e-server.mjs`, `e2e/*.spec.ts`, local SB-CUT-001 completion | Builds the dashboard, starts an isolated fixture server, and covers Team feed inspection/structured routing/reload state, Management safe defaults/one-turn authority, browser smoke, native nav reorder persistence, Workers inspection, and chat scroll behavior. | Fixture APIs are deterministic and do not invoke a live engine; a separate read-only smoke covered the rebuilt live daemon. |
| Focused React update discipline | `pnpm --filter @cuttlefish/web exec vitest run src/components/org/employee-editor.test.tsx src/components/org/employee-create-form.test.tsx src/components/auth/remote-access-panel.test.tsx --reporter=verbose` | passed on 2026-07-16: 28 tests with no React `act(...)` warnings | local repair run | Waits for async org hydration and remote-access actions before assertions or teardown. | Focused to the three suites used to reproduce the warnings. |
| Distribution contract | `pnpm --filter cuttlefish-cli exec vitest run src/cli/__tests__/distribution-contract.test.ts` | passed on 2026-07-16: 2 tests | `packages/cuttlefish/src/cli/__tests__/distribution-contract.test.ts`, local repair run | Guards agreement on bounded Node 24 support across the workspace, npm package, and Homebrew formula, and prevents the unused `classic-level` native addon from returning. | Static contract test; package installation and execution are validated separately. |
| Session registry import direction | `pnpm --filter cuttlefish-cli exec vitest run src/sessions/__tests__/registry-import-graph.test.ts src/sessions/__tests__/registry-search-messages.test.ts src/sessions/__tests__/registry-pagination.test.ts` | passed on 2026-07-16: 34 tests | `packages/cuttlefish/src/sessions/__tests__/registry-import-graph.test.ts`, local repair run | Parses runtime imports/exports in the registry façade and implementation modules, rejects cycles, and covers FTS migration/degrade/search behavior plus session search. | Registry-local architecture guard; it does not scan unrelated source subtrees. |
| Gateway files façade seam | `packages/cuttlefish/src/gateway/__tests__/files-facade-seam.test.ts` | included in `pnpm test` pass | test file + local run | Guards split `files.ts` public route behavior. | Focused to representative route flow. |
| Config schema facade seam | `pnpm --filter cuttlefish-cli exec vitest run src/shared/__tests__/config-schema-modules.test.ts src/shared/__tests__/config.test.ts src/shared/__tests__/autonomous-config.test.ts` | passed on 2026-08-23: 24 new structural tests plus the 52 pre-existing config tests | `packages/cuttlefish/src/shared/__tests__/config-schema-modules.test.ts`, local `repair/modularize-config-schema-2026-08-23` run | Guards the `config-schema.ts` -> `config-schema-*.ts` split: that `validateConfigShape` is still the single public export and is the same function identity re-exported by `shared/config.ts`, and that all 21 top-level config sections plus the notifications/connector cross-check are still dispatched to their owning module. Verified non-vacuous by mutation (removing one section's call fails the matching case). | Structural/delegation cover only; the per-key validation rules themselves remain covered by `config.test.ts` and `autonomous-config.test.ts`. |
| Artifact registry routes | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/artifact-registry.test.ts` | passed on 2026-06-26 | local artifact-registry run | Covers local artifact registration, hash/run/tag metadata, validation results, bundle manifest shape, and main API router dispatch. | Does not package bundle bytes into an archive. |
| Run attachment normalization | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/run-attachments.test.ts` | passed on 2026-06-26 | local run-attachments run | Covers normalization of artifact ids, files, folders, and URLs, plus session create/list/attach resource routes. | Does not yet cover every downstream engine's rendered attachment prompt verbatim. |
| Human checkpoints | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/checkpoints.test.ts src/gateway/__tests__/approvals.test.ts src/gateway/__tests__/work.test.ts src/sessions/__tests__/registry-approvals.test.ts` | passed on 2026-06-26 | local checkpoint run | Covers checkpoint creation, visible session pause, deferred and approved decisions, resulting-action persistence, approval compatibility, and registry helper behavior. | Does not yet cover every future producer that may emit checkpoints. |
| Run bundle export | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/run-bundles.test.ts src/gateway/__tests__/run-attachments.test.ts src/gateway/__tests__/artifact-registry.test.ts` | passed on 2026-06-26 | local run-bundle run | Covers session bundle export, required file layout, artifact copying, filtered log export, and refusal for active sessions. | Does not yet provide archive/zip packaging or import/replay execution. |
| Orchestration router façade | `packages/cuttlefish/src/gateway/__tests__/api-orchestration-routing.test.ts` | included in `pnpm test` pass | test file + local run | Guards `/api/orchestration/*` dispatch through `handleApiRequest()`. | Does not replace end-to-end live daemon tests. |
| Generic surface drift guard | `pnpm --filter cuttlefish-cli exec vitest run src/shared/__tests__/domain-drift-guard.test.ts` | passed on 2026-06-30 | local prefork common substrate Stage 0 run | Scans tracked generic surfaces (`README.md`, operator docs, templates, backend source/tests, web source/tests) for downstream program vocabulary drift in both filenames and file contents, and includes a deliberate violating fixture created during the test. | Historical/local planning and audit directories are intentionally excluded from the scan. |
| Context manager MVP focused tests | `pnpm --filter cuttlefish-cli exec vitest run src/sessions/__tests__/context-manager.test.ts src/shared/__tests__/config.test.ts src/engines/__tests__/ollama.test.ts src/engines/__tests__/kilo.test.ts src/engines/__tests__/aider.test.ts` | passed on 2026-07-05 | local context-manager MVP run | Covers mode parsing, config validation, selector preservation/drop/summary behavior, metadata shape, native-resume non-mutation, and synthetic-history engine `historyMessages` overrides. | Focused unit coverage only; full package test/typecheck are tracked separately in this session. |
| Context manager MVP root typecheck | `pnpm typecheck` | passed on 2026-07-05 | local context-manager MVP run | Confirms backend and web TypeScript compile after adding context-manager types and engine-run options. | Does not execute runtime flows. |
| Cuttlefish package test suite | `pnpm --filter cuttlefish-cli test` | failed on 2026-07-05: 1 cwd path-normalization assertion failed; 1,948 tests passed, 1 skipped | local context-manager MVP run | Broad backend vitest run after the context-manager change. | Failure was in `src/gateway/__tests__/session-query-routes.test.ts` expecting `/var/...` while runtime returned `/private/var/...`; no context-manager tests failed. |
| Smart manager delegation focused tests | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/manager-delegation-enforcement.test.ts src/sessions/__tests__/manager-delegation.test.ts src/sessions/__tests__/context.test.ts src/gateway/__tests__/run-web-session-stall-policy.test.ts` | passed on 2026-07-05: 62 tests | local manager delegation enforcement patch run | Covers manager-only delegation discipline prompt insertion, direct-report specialty roster, no "always delegate" wording, secondary-report supervisor resolution, child-session delta telemetry helper behavior, deterministic specialty-match planning, callback/explicit-inline exemptions, and the runtime enforcement path that creates a specialist child before the manager model can work inline. | Does not prove live LLM synthesis quality after the enforced child returns. |
| Smart manager delegation live harness | `pnpm build`; `pnpm cuttlefish restart`; `node packages/cuttlefish/scripts/delegation-live-harness.mjs --employee playtest-lead --timeout-ms 240000 --prompt "<split QA playtest prompt>"` | passed on 2026-07-05: parent `c25fb39a-2b3d-438a-b931-25e900d8ca0a` spawned 2 child sessions, `playtester-explorer` and `playtester-breaker`; parent and children settled idle with no `lastError` | local live gateway run after rebuild | Confirms rebuilt `dist` and running gateway can prompt a live manager with direct reports into child-session delegation for a naturally splittable task. | Used `playtest-lead` because the installed local org has no direct reports under `parliamentarian`; live model behavior remains sample evidence, not a deterministic CI assertion. |
| Smart manager delegation root typecheck | `pnpm typecheck` | passed on 2026-07-05 | local smart manager delegation patch run | Confirms backend and web TypeScript compile after adding manager delegation helper, telemetry wiring, tests, docs, and manual harness. | Does not execute live gateway behavior. |
| Cuttlefish package test suite after smart delegation patch | `pnpm --filter cuttlefish-cli test` | failed on 2026-07-05: 1 cwd path-normalization assertion failed; 1,950 tests passed, 1 skipped | local smart manager delegation patch run | Broad backend vitest run after the manager delegation change. | Failure remained in `src/gateway/__tests__/session-query-routes.test.ts` expecting `/var/...` while runtime returned `/private/var/...`; delegation tests passed. |
| Cross-department request route focused tests | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/org-cross-request-route.test.ts`; `pnpm typecheck` | passed on 2026-07-05: 2 focused tests and root typecheck | local cross-request route repair | Covers `POST /api/org/cross-request` success path creating a provider-owned session, formatting the cross-service brief, dispatching through the employee session runner, and returning 404 for unknown services. | Does not run a live engine or browser playtest. |
| Workspace profiles and department rename focused tests | `pnpm --filter cuttlefish-cli exec vitest run src/gateway/__tests__/session-write-routes.test.ts src/gateway/__tests__/org-department-rename-route.test.ts`; `pnpm --filter @cuttlefish/web exec vitest run src/components/chat/__tests__/new-chat-employee-flow.test.ts`; `pnpm typecheck`; `pnpm lint` | passed on 2026-07-05: backend 10 tests, web 13 tests, root typecheck, root lint | local workspace profile / department rename patch | Covers `workspaceProfile` session creation prompt/cwd metadata behavior, org department rename route behavior, and frontend session-create payload plumbing. | Live browser verification was not run. |
| Workspace profile config surfacing repair | `pnpm --filter cuttlefish-cli exec vitest run src/shared/__tests__/config.test.ts`; `pnpm build`; `pnpm cuttlefish restart`; `curl -fsS http://localhost:8888/api/healthz`; `curl -fsS http://localhost:8888/api/workspace-profiles`; `pnpm typecheck`; `pnpm lint` | passed on 2026-07-05: config tests 39 passed; build passed; restarted daemon PID 62915; healthz ok; live workspace profiles route returned `{"profiles":[]}`; root typecheck and lint passed | local workspace profile surfacing repair | Covers config validation accepting `workspaces.profiles`, rebuilt backend/web dist, and live daemon route availability after restart. | Active local config has no `workspaces.profiles` entries, so the picker remains hidden until profiles are configured; live browser verification was not run. |
| Seed org defaults focused tests | `pnpm --filter cuttlefish-cli exec vitest run src/cli/__tests__/config-seed.test.ts src/gateway/__tests__/org-hierarchy.test.ts` | passed on 2026-07-05: 39 tests | local seed org defaults patch run | Covers seeded HR/compliance/general persona placement, reportsTo defaults that avoid assigning everyone to Parliamentarian, new manager reporting guidance, and existing hierarchy default behavior. | Static seed/guidance coverage only; does not migrate existing user homes. |
| Seed org defaults root typecheck | `pnpm typecheck` | passed on 2026-07-05 | local seed org defaults patch run | Confirms backend and web TypeScript compile after seed/guidance/test changes. | YAML template changes are covered by focused static tests, not runtime setup execution. |
| Cuttlefish package test suite after seed org defaults patch | `pnpm --filter cuttlefish-cli test` | failed on 2026-07-05: 1 cwd path-normalization assertion failed; 1,952 tests passed, 1 skipped | local seed org defaults patch run | Broad backend vitest run after the seed org defaults change. | Failure remained in `src/gateway/__tests__/session-query-routes.test.ts` expecting `/var/...` while runtime returned `/private/var/...`; seed tests passed. |
